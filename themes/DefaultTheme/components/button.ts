import { Button } from "../../definitions/Components";
import { COLORS } from "../variables";

const BUTTON: Button = {
   FONT_COLOR: { DEFAULT: COLORS.GREY, HOVER: "#fff" }
};

export default BUTTON;
